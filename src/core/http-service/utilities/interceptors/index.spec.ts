import * as module from './';

describe('core/http-service/utilities/interceptors module', () => {
    it('should be able to import from core/http-service/utilities/interceptors as a module', () => {
        expect(module).toBeTruthy();
    });
});
