import * as module from './';

describe('core/http-service module', () => {
    it('should be able to import from core/http-service as a module', () => {
        expect(module).toBeTruthy();
    });
});
